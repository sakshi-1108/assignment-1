#include <stdio.h>
int main() {
\\decimal to binary
int ans,decNum,rem,pow;
int ans=0,pow=1;
while (decNum>0) {
int rem=decNum%2;
decNum/=2;
ans+= (rem*pow);
pow*=10;
}

\\binary to decimal
int binNum;
int ans=0,pow=1;
while (binNum>0){
rem=binNum%10;
ans+=binNum*pow
binNum/=10
pow*=2
}
